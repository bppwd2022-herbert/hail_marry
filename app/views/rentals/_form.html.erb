<%= form_with(model: rental) do |form| %>
  <% if rental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rental.errors.count, "error") %> prohibited this rental from being saved:</h2>

      <ul>
        <% rental.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :condition %>
    <%= form.text_field :condition %>
  </div>

  <div class="field">
    <%= form.label :return_date %>
    <%= form.date_select(:return_date, include_blank: true) %>
  </div>

  <div class="field">
    <%= form.label :estimate_return_date %>
    <%= form.date_select(:estimate_return_date, default: 30.days.from_now) %>
  </div>

  <div class="field">
    <%= form.label :rented_date %>
    <%= form.date_select :rented_date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :rentable_type %>
    <%= form.text_field :rentable_type %>
  </div>

  <div class="field">
    <%= form.label :rentable_id, "Rentable Id" %>
    <%= form.text_field :rentable_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>